{
  "name": "blog-server",
  "version": "1.0.0",
  "description": "blog-server",
  "main": "blog-server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bener",
  "license": "ISC",
  "dependencies": {
    "connect-history-api-fallback": "^1.6.0",
    "express": "^4.17.1"
  }
}
